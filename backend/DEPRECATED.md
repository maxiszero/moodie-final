# Deprecated — Node Express API

This folder is **no longer used** in development or production.

The canonical API is **`backend-py/`** (FastAPI + Motor + Telegram bot).

Do not run `npm run dev --workspace backend` unless you are maintaining a historical fork.
